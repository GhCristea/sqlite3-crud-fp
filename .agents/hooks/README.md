# Hooks

Hooks are automated checks that run at specific lifecycle events (e.g. before a commit).

## Available Hooks

| File | Event | Purpose |
|---|---|---|
| `pre-commit.md` | Before every commit | Typecheck, lint, format |
